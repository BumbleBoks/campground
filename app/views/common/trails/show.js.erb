$('#trail_updates').html("<%= escape_javascript(render_update_partial(@trail.updates, false)) %>")
$('.project_form').html("<%= escape_javascript(render 'common/trails/update_form') %>")
<% if logged_in? %>
	$("#other_links_button").html("<%= escape_javascript(render 'corner/favorites/add_remove_favorite', { trail: @trail }) %>")
<% end %>	
