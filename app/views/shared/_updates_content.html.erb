<% unless trail.nil? %>
<h4>Last <%= pluralize(updates.count, 'update') %> for <%= trail.name %></h4>
	<% if updates.any? %>
		<%= render_update_partial(updates, false) %>
	<% else %>
		<span class="update_content">
			No updates found
		</span>
	<% end %>
<% end %>
